let names = ['Diego', 'Karol', 'Cristopher'];

for(let index in names){//el complemento in se usa para encontrar el indice del elemento en el array
    console.log(index)
}

for(let name of names){//el complemento of se usa para encontrar el valor del elemento en el array
    console.log(name)
}

